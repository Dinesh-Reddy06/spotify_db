# spotify_db
A fully normalized relational database system inspired by Spotify, implemented using SQL. The project models core entities such as users, artists, albums, songs, playlists, and playback history. It incorporates primary/foreign key constraints, joins, views, and advanced queries to simulate real-world music streaming operations.
